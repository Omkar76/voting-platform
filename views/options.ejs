<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="csrf-token" content="<%= csrfToken %>">
    <title>Options</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="flex flex-col justify-center gap-y-2.5 p-4 max-w-xl m-auto">
    <h1 class="text-xl">Options for <%= question.title %></h1>
    <form method="POST" action="/elections/<%= question.electionId %>/questions/<%= question.id %>/options" enctype="application/json" class="flex flex-row gap-1">
        <input type="hidden" name="_csrf" class="flex-grow value="<%= csrfToken %>">
        <input  class="p-2 border-2 border-gray-dark rounded-sm w-full" type="text" name="text"/>
        <button class="uppercase bg-purple-800 text-white rounded-md font-bold p-2">
            Add&nbspOption
        </button>
    </form>
    
    <% if(question.options) { %>
        <% for( let o of question.options)  { %>
        <div class="border text-center text-lg">
          <%= o.text  %> 
        </div>
        <% } %>
    <%}%>
    </div>
</body>
</html>