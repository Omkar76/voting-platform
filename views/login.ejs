<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="csrf-token" content="<%= csrfToken %>">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        Sign in
    </title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-slate-200">    
    <%- include('header') %>
    <div class="flex flex-col justify-center gap-y-2.5 p-4 max-w-sm m-auto">
        <%- include('error') %>

        <h1 class="text-4xl text-purple-800 font-bold">Sign in</h1>
        <form action="/login" method="post" class="flex flex-col gap-2 text-purple-800">
            <input type="hidden" name="_csrf" value="<%= csrfToken %>">
            <label>
                Email
                <input placeholder="john@example.com" class="w-full border-2 rounded-sm border-purple p-2" type="email" name="email">
            </label>
            <label>
                Password
                <input placeholder="******" class="w-full border-2 rounded-sm border-purple p-2" type="password" name="password">
            </label>

            <button class="w-full bg-purple-600 p-2 text-white rounded-md">Submit</button>


        </form>
        <p class="py-2 text-gray-700">New user? <a href="/signup" class="text-purple-800">Signup now</a></p>

        
    </div>
</body>

</html>